version: 2.1

jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-node-browsers
    
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo hello world
      - run:
          name: Show current time
          command: date
